export default function Main() {
    const name = "blabla";
    const id = "blabla";

    return (
        <main>
            <h1>메인페이지입니다.</h1>

            <p>사용자 이름 : {name}</p>
            <p>사용자 아이디 : {id}</p>
        </main>
    );
}
